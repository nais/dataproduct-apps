apiVersion: "nais.io/v1"
kind: "NaisJob"
metadata:
  name: dataproduct-apps
  namespace: nais
  labels:
    team: nais
spec:
  image: {{ image }}
  schedule: "13 6 * * *"
  liveness:
    path: /isHealthy
  readiness:
    path: /isReady
